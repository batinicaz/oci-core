name: Create Release
on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    uses: batinicaz/gha/.github/workflows/release.yml@4adcb748995f19aa9991c41ecdcdd3350dae9707 # ratchet:batinicaz/gha/.github/workflows/release.yml@latest
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_INSTALL_ID: ${{ secrets.APP_INSTALL_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
